# Public base URL of the deployed snap (no trailing slash)
# Required in production so button target URLs resolve correctly
SNAP_PUBLIC_BASE_URL=https://your-snap.host.neynar.app

# Set to 1 during local development to skip JFS signature verification
# SKIP_JFS_VERIFICATION=1

# Port for local dev server (default: 3003)
# PORT=3003
